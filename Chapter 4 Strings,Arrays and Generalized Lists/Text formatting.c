/*
问题描述：处理待格式化文本，按用户指定版面规格重排版面（页内调整、分段、分页等），结果存入输出文件。
基本要求（续）：若每页页底空行数不少于 3，页号印在页底空行中第 2 行中间位置，否则不印；版面参数包含页长、页宽、左空白、头长、脚长、起始页号。
测试数据：略（注意标点后加空格）。
实现提示：设左空白数 ×2 + 页宽 ≤ 160，设置行缓冲区，先实现GetAWord(w)操作处理行尾、文件尾、多余空格压缩等。
选作内容：
输入和输出文件名由用户指定。
允许用户指定右对齐（多余空格均匀分布在字之间）。
实现字符填充技术（原文本中 “@” 用两个 “@” 表示）。
允许用户自动按多栏印出一页。
*/